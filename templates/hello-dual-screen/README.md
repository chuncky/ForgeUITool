# Hello dual-screen template for ForgeUI Kit (qm10xd / LVGL 9.10)

双页示例：默认屏 **Home**，Next → Settings，Back → Home。

样式约定与当前产品一致：
- 颜色 `#RRGGBBAA`；独立 `*_opacity` / `bg_img_opacity` 默认 `255`
- 默认字体 `@SourceHanSansCN-Bold`、字号 `16`
- 按钮使用 primary 种子；标签透明底 + 浅色字（适配深色屏）
- 生成物目录：`forgeui_generated/`

# 内置字体（设计器属性默认 / 画布）

路径：`xos-package/res/ttf/`

| 文件 | 用途 |
|------|------|
| `SourceHanSansCN-Bold.ttf` | **默认字族**（`text_font=@SourceHanSansCN-Bold`） |
| `2312_v9.ttf` | 可选内置字族 |

默认字号：`text_font_size=16`。新建/打开工程时复制到 `assets/fonts/` 并写入 `project.assets.fonts`。

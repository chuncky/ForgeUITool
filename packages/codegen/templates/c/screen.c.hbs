#include "screen_{{screen.id}}.h"
{{#each screen.imageIncludes}}
#include "../image/{{this}}.h"
{{/each}}
{{#each screen.fontIncludes}}
#include "../fonts/{{this}}.h"
{{/each}}

static lv_obj_t *{{screen.screenSym}};

{{#each screen.symbols}}
lv_obj_t *{{this}};
{{/each}}

lv_obj_t *{{ir.screenPrefix}}{{screen.id}}_get(void)
{
    return {{screen.screenSym}};
}

void {{ir.screenPrefix}}{{screen.id}}_create(void)
{
{{{screen.body}}}
}

#include "ui_events.h"

{{#each handlers}}
{{#if ../weak}}__attribute__((weak))
{{/if}}void {{this}}(void)
{
    /* TODO: implement business logic (ForgeUI will not overwrite this file once present; new handlers are appended) */
    {{#if ../weak}}/* FR-056 weak stub — firmware may override */{{/if}}
}

{{/each}}

---
description: 任何功能/契约改动必须先改文档，再改代码，最后验证通过
alwaysApply: true
---

# 文档先行 → 代码 → 验证

用户要求：**以后任何修改，都要先改文档，然后再改代码，改好代码都要验证通过。**

## 强制顺序（不可跳过）

1. **先改文档**  
   - 在动手改代码前，先更新对应契约/详设/手册/进度中的相关章节（写清行为、字段、CodeGen、验收）。  
   - 大改或对标竞品时：先在文档里给出方案要点，再实现。  
   - 禁止「代码已合、文档后补」作为默认路径。

2. **再改代码**  
   - 实现必须与刚更新的文档一致。  
   - 触及 `project.json` 字段时同步 `types` + `schemas/project.schema.json` + 契约测试字段列表。

3. **再验证通过**  
   - 跑与改动相关的 vitest；涉及 schema/codegen/designer/preview 时再跑：

```bash
npm run test:pipeline
```

   - 未通过不得宣称完成。  
   - 需要完整 LVGL 工具链的编译可 soft-skip，但 validate/generate/相关单测必须绿。

## 文档落点（按改动类型选）

| 改动 | 优先更新 |
|------|----------|
| 控件属性/样式/长文本/字号 | `docs/控件属性面板详设/*.md`、手册对应节、`嵌入式UI工具_控件属性面板详细设计说明.md` |
| 需求验收口径 | `docs/嵌入式UI工具_设计需求文档.md`（相关 FR） |
| 画布/设计器行为 | `docs/软件详细设计/09-设计器界面详细设计.md` 等 |
| 进度与回归入口 | `docs/IMPLEMENTATION_PROGRESS.md` |
| 生成/管道 | 必要时更新详设 + 保持 `.cursor/rules/pipeline-validate-generate.mdc` |

## 禁止

- 先写一堆代码再回头补文档  
- 只改画布/UI、不改文档与 CodeGen/测试契约  
- 验证失败仍回复「已完成」
